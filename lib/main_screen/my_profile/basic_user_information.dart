import 'package:flutter/material.dart';

class BasicUserInformation extends StatelessWidget {
  const BasicUserInformation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        // RichText(text: ),
        const SizedBox(
          height: 7,
        ),
      ],
    );
  }
}
